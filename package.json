{
  "name": "generate-text-video",
  "version": "0.0.1",
  "description": "",
  "private": true,
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "deploy": "touch build/.nojekyll && gh-pages -d build -t"
  },
  "author": "succie",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "snowpack": "^3.0.13",
    "typescript": "^4.2.3"
  }
}
